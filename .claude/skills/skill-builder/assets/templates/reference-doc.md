# <Topic> reference

<One line on what this file covers, so the agent can confirm it opened the
right one.>

Loaded on demand from SKILL.md. Keep this file focused on a single topic — the
agent pays its full cost the moment it reads it. If this grows past a few
hundred lines, split it and give each file its own trigger in SKILL.md.

## <Section>

<Detail that does not apply on every run: full API surfaces, error-code tables,
schema listings, uncommon edge cases, worked examples for rare inputs.>

## <Section>

<More detail.>
